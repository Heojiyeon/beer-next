import BottomNavBar from '@/components/bottomNavBar';
import Header from '@/components/header';
import Profile from '@/components/profile';
import '@/styles/globals.css';
import type { Metadata } from 'next';
import localFont from 'next/font/local';

export const metadata: Metadata = {
  title: 'beer-next',
  description: 'Generated by next15, React19',
};

const font = localFont({
  src: '../fonts/PretendardVariable.ttf',
  display: 'swap',
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ko" className={font.className}>
      <body className="min-h-screen">
        <header>
          <Header />
        </header>

        <div className="flex justify-around md:m-4 py-5 md:py-10">
          <div className="md:block hidden flex-[0.3] justify-center items-start">
            <Profile />
          </div>
          <main className="flex-1 md:flex-[0.7] flex justify-center items-center">
            {children}
          </main>
        </div>

        <footer>
          <BottomNavBar />
        </footer>
      </body>
    </html>
  );
}
